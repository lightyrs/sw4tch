- if current_user == @swatch.user
  .form-actions
    = link_to 'Edit', edit_swatch_path(@swatch), class: 'btn'
    .pull-right= link_to 'Delete', swatch_path(@swatch), method: :delete, confirm: 'Are you sure?', class: 'btn btn-danger'
