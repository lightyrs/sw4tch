%iframe.swatch-frame

  - if swatch.persisted?

    .swatch-wrapper{class: "swatch-#{swatch.id}"}
      %a.swatch{href: swatch_path(swatch)}
        %div.specimen

  - else

    .swatch-wrapper
      .swatch
        %div.specimen
