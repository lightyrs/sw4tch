.page-header
  .heading.pull-left
    %h2= @swatchbook.name
    %p.byline
      #{@swatchbook.created_at.strftime("%b %d, %Y")} <i>by</i> #{link_to @swatchbook.user.try(:username), user_path(@swatchbook.user)}

#swatches.index
  = render collection: @swatches, partial: 'swatches/swatch'

.pagination= will_paginate @swatches
