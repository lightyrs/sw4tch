#sidebar.span3

  %h1#sw4tch= link_to 'sw4tch', root_path

  = render 'shared/flash', flash: flash if flash.present?

  - if current_user
    = render 'shared/session'
    = link_to 'New Swatch', new_swatch_path, class: 'btn btn-block btn-primary btn-large'

  - else
    = render 'home/welcome'
